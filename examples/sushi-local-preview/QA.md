# 历史验证记录：圆阵列版本 · 2026-09-05

> 下列记录属于已经被用户否定的圆阵列试验版，不代表当前页面。当前版本已恢复四条双成员超边，改成逐条浏览；当前测试见 [QA-reader.md](QA-reader.md)。历史记录保留以便区分版本和验证范围。

本例保留 35 个独立实体、14 条高阶关系、57 条成员关联；每条关系至少有三个不同成员。原始文档和 `sushi-document-test` 页面未改动，没有推送到 GitHub。

## 验证结果

- 正式 Bundle showcase 校验：609 项，无错误或警告。每条证据与原文行范围一致；这不等于历史事实独立核验。
- 离线代码测试：556 passed、4 skipped、6 deselected；未运行联网集成测试。
- 真实 Chrome 完整交互检查：2,071 次鼠标点击、885 个状态、24,481 项检查，零失败、零运行时错误。
- 覆盖全部 35 个节点、14 条超边、57 个矩阵成员单元格和 57 个圆内节点显示位置；包含三视图切换、超边标题/填充/边界、关联线、详情栏跳转、拖动/重置、键盘、语言和主题切换。
- 最终悬停样式差异复测：37 个状态、202 项检查，零失败。覆盖全部 14 条超边的明暗主题，以及三个视图在 1440、1920、390 宽度下的显示。
- 最终活动边透明度为 1、线宽 3.2px；无关包络层透明度为 0.08。核对实际计算样式，不只检查 CSS 类名。
- 已目视检查圆形总览、复杂单超边、三苏成员展开、悬停和窄屏截图。1920 宽度使用 7×2 阵列；窄屏保留滚动以维持可读字号。是否接受共享实体的重复展示，仍以用户本地试用反馈为准。

## 本次修复

1. 从本高阶示例中移出四条二元关系及仅由其引用的实体，不补造第三个成员。
2. 规整圆阵列沿用相同实体 ID 的显示副本，不复制源实体或改变成员集合；拖动旋转成员位置，不拉歪圆。
3. 修复跨视图的陈旧选择、悬空连线和过大的连线焦点框。
4. 修复提示框改变滚动区域高度导致的矩阵误选：独立复现中修改前 8/8 错位，修改后 8/8 正确。
5. 修复悬停虚化样式被覆盖，确认填充和包络线真实变色/变淡。

## 可追溯范围

完整点击检查对应 HTML SHA256：

`5dbc60ab1c27e4767cb139f091deb00c99cf4d0e40f5b9c60e3f96e27111c4f3`

最终预览 HTML SHA256：

`7a4cc2b9d936250051f131c5486bf569a1f99fcaf9d882a01d31c84b5152438a`

两者仅相差一行悬停 CSS，CSS 以外逐字一致；该差异另做全部超边悬停和响应式复测，没有把不同产物的检查混称为同一次测试。

复测脚本：`tools/check_workbench_interactions.mjs` 和 `tools/check_workbench_hover.mjs`。原始报告在本机 `D:/CodexData/.codex/visualizations/2026/08/14/019ffe48-ac16-7073-ad6a-112bf0321a85/highorder-symmetry-20260905/` 下的 `qa-final/interaction-report.json` 与 `qa-css-delta/hover-delta-report.json`。

修改文件的 Ruff 检查通过。扩大到全仓旧测试目录的检查仍有 51 条既有 lint 提示、14 个旧文件格式提示；这些文件本次未改动，因此不宣称全仓 lint 全绿。

本地 Skill 已按受管流程更新并通过深度健康检查，旧版备份为 `D:/CodexData/.codex/skills/.hyper-knowledge.backup-20260905T040925Z`；未修改或重装 Codex 应用。
