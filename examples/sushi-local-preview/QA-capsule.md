# 胶囊超边 · 独立本地试验

页面：`views/capsule-trial/workbench.html`。保留原 `views/workbench.html` 与发布示例，不自动发布、不推送 GitHub。

## 视觉与语义

- 全图仍是 39 个唯一实体、18 条原始超边、65 条成员连线；包括原有的双成员超边。数据、角色和证据没有改写。
- 节点仍为圆形，沿用全图参与度尺寸和共享节点虚线环。超边改为横向胶囊，左侧 E 编号、右侧名称；当前 18 个名称均完整单行显示。
- 淡色底、细描边与小幅柔和阴影区分超边和背景；焦点加强填色与阴影，但不缩放、移动胶囊。
- 编号沿全图稳定排序生成，悬停、选中、语言切换不会重新编号；它是展示索引，不是重要性排名，也不替换原始超边 ID。
- 成员曲线在胶囊圆角边界外约 1 像素结束，不穿过内部名称。轮廓变化不产生二元事实或辅助关系。
- 悬停超边时标出其所有成员角色；聚焦节点时仅标该节点自己的成员连线。例如苏轼显示 18 条自身角色，不把全图 65 条线全部加字。
- 角色来自已有记录，不生成新的“原因”或因果断言。文字优先放在线上，必要时用短引线微调位置；提示框位于画布左上角，避开角色标签。
- 其他元素只淡化，不删除或重新排列。详情栏展开会改变画布可用宽度并缩放整图；布局坐标保持不变，不承诺屏幕像素位置不动。
- 矩阵、关联视图和逐条包络阅读器保持原有模式。小屏在画布内滚动，不把整张图无限缩小。
- 点击后的浏览器默认黑色方框已移除。键盘焦点改用圆点或胶囊自身的描边，并修复详情栏按钮重建后返回总览焦点的问题。

## 确定性验证

- 正式 Bundle 校验：729 项通过，0 错误、0 警告。
- 25 组真实渲染器契约通过，包含规范 ID、双成员超边、多角色去重、孤立节点、排序与选择稳定性，以及详情栏焦点恢复。
- 171 个角色焦点几何案例：真实角色集合准确，避开节点安全圈、胶囊和其他角色文字。
- 195 个成员曲线截断案例：到胶囊边界的间距正确，保留原二次曲线的子段。
- 完整离线回归：563 passed、4 skipped、6 deselected；3 条既有依赖警告。代码与测试 Ruff 检查通过。

## 浏览器与目视

浏览器产物目录：`D:/CodexData/.codex/visualizations/2026/08/14/019ffe48-ac16-7073-ad6a-112bf0321a85/capsule-trial-20260905/`。

`full-v1/capsule-trial-report.json` 对应焦点细修前 `00adce…9737`，7,150 项检查、435 个状态、484 次真实鼠标交互全部通过，0 错误、0 外网请求。覆盖全部 39 节点、18 超边和 65 成员连线，E 编号点选、角色全文与避障、逐条阅读返回、矩阵和关联视图往返、平移缩放重置、1920 / 1440 / 390 宽度及浅深主题。该版 HTML 已单独存档在 `full-v1/workbench.html`。

焦点补丁只改变默认方框和键盘焦点恢复；与上述归档逐行比对，没有数据、布局、成员曲线或其他视觉变化。`focus-final/capsule-trial-report.json` 对应 `61c7…9f9a`，3,513 项检查、179 个状态、251 条鼠标/键盘交互记录通过。覆盖全部 39 节点、18 胶囊和编号、Enter / Space 激活、详情栏返回图形的焦点恢复、悬停恢复及桌面和移动端提示框，0 错误、0 外网请求。

额外实际点击连线发现浏览器也会给 SVG 路径加默认方框。最终 `73be…e9be` 仅追加一条连线焦点去方框的选择器；移除该选择器可精确还原 `61c7…9f9a`。`final-73be/focus-diagnosis.json` 核验 3 条实际点击的外围 / 中心 / 共享节点连线均不再出现方框，节点与胶囊的键盘焦点仍分别沿自身轮廓显示 4 / 3 像素描边，0 错误、0 外网请求。此差异单独核验，不将历史摘要的全量测试冒充最终摘要的全量复测。最终页重新运行针对性回归：11 passed；Ruff 检查通过。

目视已检查浅深总览、乌台诗案角色、苏轼 18 条自身角色和苏辙 4 条自身角色；最终摘要的总览、苏辙、治理西湖及键盘焦点截图保存在 `final-73be/`。标签、编号无文字重叠，阴影与文字层次可辨；这是开发者目视检查，不等于用户审美确认。

## 冻结摘要

- 新胶囊页：`73be8b77c49e7d7add0a84b00829d6bc4226baba2fdc46cde03efa159b9ae9be`。
- 原总览页：`7652b2a8fd07d7c7927fe1bc1d24fe6284851cd465ea64c834c73e41c6b8e254`，保持不变。
- 原发布示例：`9bef260dbde2472bbc393779ca71b9f3f3ca499611314522d738c582077b487c`，保持不变。
- 原文副本：`65869e3042b4a3b7a83695f1a053f8a2d99a09d031167770d4636e433442c0ed`，保持不变。

复测命令：

```powershell
.\.venv\Scripts\python.exe -m pytest -q -m 'not integration'
node tools/check_hypergraph_capsule_trial.mjs examples/sushi-local-preview/views/capsule-trial/workbench.html examples/sushi-local-preview/bundle OUTPUT full
```

浏览器检查使用隔离 Chrome、实际鼠标交互与视口截图，不接触个人浏览器资料；通过 `HK_PLAYWRIGHT_MODULE` 指向本机已有 Playwright 依赖。该检查与应用内浏览器插件是否可连接无关。
